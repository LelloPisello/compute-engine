#version 450
layout(local_size_x = 1) in;

layout (binding = 0) buffer INPUT {
    int INdata[];
};
layout (binding = 1) buffer OUT {
    int OUTdata[];
};

void main() {
    OUTdata[gl_GlobalInvocationID.x] = int(gl_GlobalInvocationID.x);
}